node_modules/
dist/
.vite/
.superpowers/
.astro/
__pycache__/
*.local
playwright-report/
test-results/
supabase/.temp/
scripts/search-eval/judge.html

# Scraper output — lives in Supabase, not git.
# The scraper still writes these locally during a run but they are not committed.
public/data/items/
public/data/archive/
public/data/manifest.json
public/data/archive-manifest.json

# Exception (#242 NOW #1): the combined, gzipped active-lots artifact IS
# committed — it's the CDN-served static read model for the active grid, built
# into dist/ by `npm run build` and served from GitHub Pages so the browser
# reads it instead of the write-contended Supabase `lots` table.
!public/data/active-lots.ndjson.gz
