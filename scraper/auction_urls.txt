# Cannon's Auction URLs -- optional manual fallback list.
#
# The scheduled scraper discovers current auctions from Cannon's public auction
# listing by default. Add full AuctionItems URLs here only when discovery is
# broken or you need to force-scrape a specific auction.
#
# Required format, one URL per line:
# https://bid.cannonsauctions.com/Public/Auction/AuctionItems?AuctionId=...
